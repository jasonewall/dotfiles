#!/usr/bin/env bash

for feature in ps1 git k8s-context rbenv atom jenv ivy-cli shimmer k8s profile wsl-keychain wslprofile; do
    if [ -f ~/.dotfiles/dotfiles-$feature ]; then
        source ~/.dotfiles/dotfiles-$feature
    fi
done
